@model System.Data.DataTable
@{
    ViewBag.Title = "Chi tiáº¿t thiáº¿t bá»‹";
    var row = Model.Rows[0];
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h3 class="mb-0"><i class="fa fa-info-circle text-primary"></i> Chi Tiáº¿t Thiáº¿t Bá»‹</h3>
        <a class="btn btn-outline-secondary btn-sm" href="@Url.Action("QuanLy", "ThietBi")">â† Quay láº¡i danh sĂ¡ch</a>
    </div>

    @if (ViewBag.TenKhoaPhongBan != null && !string.IsNullOrWhiteSpace(ViewBag.TenKhoaPhongBan.ToString()))
    {
        <div class="alert alert-info py-2">ÄÆ¡n vá»‹: <strong>@ViewBag.TenKhoaPhongBan</strong></div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger py-2">@TempData["Error"]</div>
    }

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success py-2">@TempData["Success"]</div>
    }

    <div class="card shadow-sm mb-3">
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-6">
                    <div><strong>MĂ£ thiáº¿t bá»‹:</strong> @row["ID_ThietBi"]</div>
                </div>
                <div class="col-md-6">
                    <div><strong>TĂªn thiáº¿t bá»‹:</strong> @row["TenTB"]</div>
                </div>
                <div class="col-md-6">
                    <div><strong>Danh má»¥c:</strong> @row["TenDanhMuc"]</div>
                </div>
                <div class="col-md-6">
                    <div><strong>Tráº¡ng thĂ¡i:</strong> @row["TrangThaiTB"]</div>
                </div>
                <div class="col-md-6">
                    <div><strong>GiĂ¡ trá»‹:</strong> @string.Format("{0:N0} Ä‘", row["Gia"])</div>
                </div>
                <div class="col-md-6">
                    <div><strong>Sá»‘ seri:</strong> @row["SoSeri"]</div>
                </div>
                <div class="col-md-6">
                    <div><strong>NhĂ  cung cáº¥p:</strong> @row["TenNhaCC"]</div>
                </div>
                <div class="col-md-6">
                    <div><strong>Khoa/PhĂ²ng ban:</strong> @row["TenKhoaPhongBan"]</div>
                </div>
                <div class="col-12">
                    <div><strong>ThĂ´ng sá»‘ ká»¹ thuáº­t:</strong></div>
                    <div class="border rounded p-2 bg-light">@row["ThongSoKT"]</div>
                </div>
            </div>
        </div>
    </div>

    <div class="card border-danger shadow-sm">
        <div class="card-header bg-danger text-white py-2">
            <strong>BĂ¡o há»ng thiáº¿t bá»‹</strong>
        </div>
        <div class="card-body">
            @using (Html.BeginForm("BaoHong", "TruongKhoa", FormMethod.Post))
            {
                <input type="hidden" name="id" value="@row["ID_ThietBi"]" />
                <input type="hidden" name="fromDetail" value="true" />

                <div class="mb-3">
                    <label class="form-label">MĂ´ táº£ hÆ° há»ng *</label>
                    <textarea name="mota" class="form-control" rows="3" placeholder="Nháº­p mĂ´ táº£ chi tiáº¿t lá»—i cá»§a thiáº¿t bá»‹..." required></textarea>
                </div>

                <button type="submit" class="btn btn-danger">Gá»­i bĂ¡o há»ng</button>
            }
        </div>
    </div>
</div>

